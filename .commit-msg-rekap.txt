fix: rekap voucher filter — remove duplicate All options, default to all data

Issues fixed:
- Dropdown agent/profile had TWO "All" options (value="" and value="all")
  — confusing UX, removed the duplicate
- Default period mode was 'monthly' with current month — if no vouchers
  existed in the current month, the page showed no data and the filter
  appeared broken. Changed default to 'all' (show all data)
- Search filter was not null-safe — item.profile.name.toLowerCase() would
  crash if profile was null. Added optional chaining

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
