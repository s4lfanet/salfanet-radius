# ============================================
# ZTE C320 OLT Management System - Dependencies
# ============================================
#
# Python Version Requirement:
# - Recommended: Python 3.10, 3.11, or 3.12
# - Supported: Python 3.13+ (uses vendored telnetlib)
# - NOT supported: Python 3.9 or older
#
# Core Dependencies:
# - telnetlib: Built-in library (no install needed)
#   * Python 3.10-3.12: Uses stdlib version
#   * Python 3.13+: Uses vendored version from core/vendor/telnetlib.py
#   Used for OLT Telnet communication
#
# External Dependencies:
# ============================================

# Environment configuration
python-dotenv>=1.0.0

# Telegram Bot Integration (optional - only needed for Telegram Bot)
# Install with: pip install python-telegram-bot
# Uncomment below for Telegram bot support:
# python-telegram-bot>=20.7

# API Dependencies (optional - only needed for Web UI)
# Install with: pip install -r api/requirements.txt
# Or uncomment below for full installation:
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# python-multipart>=0.0.6
# websockets>=12.0
# python-socketio>=5.10.0

# ============================================
# Installation:
#   CLI Only:  pip install -r requirements.txt
#   Full (Web UI): pip install -r requirements.txt -r api/requirements.txt
#
# Verify Python version:
#   python --version
#   (Should show 3.10+, recommended 3.10-3.12)
# ============================================
