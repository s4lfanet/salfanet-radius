# Auto-generated by salfanet-radius - DO NOT EDIT MANUALLY

client nas_coba {
    ipaddr = 10.20.30.13
    secret = n6l3qS5JCIDyUywI
    shortname = coba
    nas_type = other
    require_message_authenticator = no
    limit_proxy_state = no
}

client nas_server {
    ipaddr = 10.20.30.10
    secret = secret123
    shortname = server
    nas_type = other
    require_message_authenticator = no
    limit_proxy_state = no
}

client nas_testmikrotik {
    ipaddr = 10.20.30.11
    secret = PIQTqHhO22A6vkdu
    shortname = testmikrotik
    nas_type = other
    require_message_authenticator = no
    limit_proxy_state = no
}

client nas_dstpaska {
    ipaddr = 10.20.30.12
    secret = jXXBZ7JDDwEhiOul
    shortname = dstpaska
    nas_type = other
    require_message_authenticator = no
    limit_proxy_state = no
}

# --- VPN Server Gateway Clients ---
# When CHR VPN server masquerades traffic, all NAS packets
# arrive from the gateway IP. All NAS behind the same VPN
# server MUST use the same RADIUS shared secret.
# If secrets differ, configure a NAT bypass on the CHR for RADIUS UDP ports.

# SKIPPED: vpn_gw_server_chr — NAS secrets differ (3 unique secrets).
# Add a CHR NAT bypass rule: srcnat accept, protocol=udp, dst-port=1812-1813, dst=10.20.30.10
